import { Story, Status } from './../../../.storybook/components';
import { Props } from '@storybook/addon-docs/blocks';

# Select

<Status />

<Story id="components-select--base" />

<Props id="components-select--base" />

## With Icons

<Story id="components-select--icon" />

### With Icons in the list (left)

<Story id="components-select--icon-left" />

### With Icons in the list (right)

<Story id="components-select--icon-right" />

## With Helper Text

<Story id="components-select--helper-text" />

## size

`size` property controls the size in height.

The default `size` property is set to `md`.

| Size | Design Token Reference | Height |
| ---- | ---------------------- | ------ |
| `sm` | small                  | 36px   |
| `md` | medium                 | 48px   |

## Events

| Event          | Description                                        | Type                  |
| -------------- | -------------------------------------------------- | --------------------- |
| `apolloChange` | Evento disparado quando o valor do componente muda | `CustomEvent<string>` |

### Orientações:

Para ter acesso ao valor do evento emitido deve ser usada a propriedade `detail` do objeto `CustomEvent` que é emitido nos métodos.
