import { Story, Status } from './../../../.storybook/components';
import { Props } from '@storybook/addon-docs/blocks';

# Collapse

<Status />

O `apollo-collapse` é útil para reduzir o espaço vertical com grandes quantidades de informações. A funcionalidade padrão do componente é de permanecer aberto até que seja explicitamente fechado.

<Story id="components-collapse--base" />

<Props id="components-collapse--base" />
