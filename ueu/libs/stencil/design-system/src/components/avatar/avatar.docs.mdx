import { Story, Status, Headings } from './../../../.storybook/components';
import { Props } from '@storybook/addon-docs/blocks';

# Avatar

<Status />{' '}

O componente `apollo-avatar` é usado para renderizar avatar de um usuário.

<Story id="components-avatar--base" />

<Props id="components-avatar--base" />
