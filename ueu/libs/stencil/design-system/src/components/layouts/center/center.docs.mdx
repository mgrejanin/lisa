import { Story, Status } from './../../../../.storybook/components';
import { Props } from '@storybook/addon-docs/blocks';

# Center

<Status />

O `apollo-center` é um componente de layout que centraliza seu filho dentro de si mesmo.

<Story id="layouts-center--base" />

<Props id="layouts-center--base" />

## Width, Height

Coloque qualquer elemento filho dentro dele, dê a ele qualquer `width` ou / e `height`, isso garantirá que o filho esteja centralizado.

<Story id="layouts-center--width-height" />
