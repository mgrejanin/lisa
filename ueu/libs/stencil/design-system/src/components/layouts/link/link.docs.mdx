import { Story, Status } from './../../../../.storybook/components';
import { Props } from '@storybook/addon-docs/blocks';

# Link

<Status />

O `apollo-link` é um componente usados ​​principalmente para navegação. Este componente é estilizado para se parecer com um hiperlink.

Basta passar a propriedade `isExternal` para ser aberto em uma nova aba.

<Story id="navigation-link--base" />

<Props id="navigation-link--base" />
