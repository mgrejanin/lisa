import { Story, Status } from './../../../../.storybook/components';
import { Props } from '@storybook/addon-docs/blocks';

# Toolbar

<Status variant="inDevelopment" />

O component `apollo-toolbar` fornece conteúdo e ações relacionadas ã tela atual. É usado para navegação e ações.

<Story id="surface-toolbar--base" />

<Props id="surface-toolbar--base" />

## Search

Com um campo de pesquisa principal, basta fornecer uma prop `is-search`

<Story id="surface-toolbar--search" />

## Actions

Use `apollo-toolbar-actions` para ações na barra

<Story id="surface-toolbar--actions" />

## Menu

Use `apollo-menu-item` para uso na navegação

<Story id="surface-toolbar--menu" />

## Events

| Event                | Description                                                                                               | Type                      |
| -------------------- | --------------------------------------------------------------------------------------------------------- | ------------------------- |
| `apolloSearchBlur`   | Evento disparado quando o campo perde o foco                                                              | `CustomEvent<FocusEvent>` |
| `apolloSearchChange` | Evento disparado quando o usuário termina de digitar e o tempo de atraso de envio do evento for concluído | `CustomEvent<InputEvent>` |
| `apolloSearchFocus`  | Evento disparado quando o campo recebe foco                                                               |

### Orientações:

Para ter acesso ao valor do evento emitido deve ser usada a propriedade `detail` do objeto `CustomEvent` que é emitido nos métodos.
