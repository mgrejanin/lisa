import { Story, Status } from './../../../.storybook/components';
import { Props } from '@storybook/addon-docs/blocks';

# Snackbar

<Status />

<Story id="components-snackbar--base" />

<Props id="components-snackbar--base" />

## Events

| Event               | Description                                         | Type                      |
| ------------------- | --------------------------------------------------- | ------------------------- |
| `actionButtonClick` | Evento disparado quando o botão de ação for clicado | `CustomEvent<MouseEvent>` |

### Orientações:

Para ter acesso ao valor do evento emitido deve ser usada a propriedade `detail` do objeto `CustomEvent` que é emitido nos métodos.

## Methods

### `close() => Promise<void>`

API para fechar o snackbar

#### Returns

Type: `Promise<void>`

### `open(timeout?: number) => Promise<void>`

API para abrir o snackbar

#### Returns

Type: `Promise<void>`

### `setLabel(label: string) => Promise<void>`

API para alterar a label do snackbar

#### Returns

Type: `Promise<void>`

### Orientações para angular:

Para utilizar os métodos do componente utilize o @ViewChild() conforme exemplo a seguir:

Html:

```html
<apollo-button (onClick)="open()">Show snackbar</apollo-button> <apollo-snackbar #snackbar />
```

Ts:

```javascript
@ViewChild('snackbar') snackbar: ApolloSnackbar;

//...

open() {
    this.snackbar.open();
}
```
