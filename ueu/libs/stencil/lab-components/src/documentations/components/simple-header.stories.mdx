import { Meta, Story, Canvas, Source, Description } from '@storybook/addon-docs/blocks';
import readme from './../../components/simple-header/readme.md';

<Meta title="Components/Simple Header" />

export const pageTitle = 'Detalhes';

export const Template = props => (
    <div style={{ width: 500 }}>
        <lab-simple-header {...props} is-editing={props.isEditing} page-title={props.pageTitle} />
    </div>
);

# Simple Header

## All Props

<Canvas>
    <Story
        name="All Props"
        args={{
            pageTitle: pageTitle,
            isEditing: false,
        }}
        parameters={{
            docs: {
                source: {
                    code: `<lab-simple-header pageTitle="${pageTitle}"/>`,
                },
            },
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

<Description markdown={readme} />
