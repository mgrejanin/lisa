/*
 * If you want to use a local mock to run the docs page, simply add your openApi json inside the below object
 */
const json = {};

export const generateMockJson = (): Record<string, unknown> => json;
