export enum StatusExtract {
    cancelled = 'Cancelado',
    processing = 'Processando',
    error = 'Erro ao efetuar',
}

export enum StatusExtractItem {
    judicial_block_transferred = 'judicial_block_transferred',
}
