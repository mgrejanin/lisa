export class CommomElements {
    MAT_INPUT_ERROR = (): string => {
        return '.mat-error';
    };

    MAT_OPTION_ITEM = (): string => {
        return 'mat-option';
    };

    MAT_CALENDAR_TODAY_DATE = (): string => {
        return '.mat-calendar-body-today';
    };

    MAT_CALENDAR_DATE = (): string => {
        return '.mat-calendar-body-cell';
    };

    MAT_CALENDAR_DATE_DISABLE = (): string => {
        return 'mat-calendar-body-disabled';
    };

    MAT_CALENDAR_DATE_VALUE = (): string => {
        return '.mat-calendar-body-cell-content';
    };

    MAT_BUTTON_DISABLE = (): string => {
        return 'mat-button-disabled';
    };

    MAT_PREVIOUS_CALENDAR_DATE = (): string => {
        return '.mat-calendar-previous-button';
    };

    MAT_NEXT_CALENDAR_DATE = (): string => {
        return '.mat-calendar-next-button';
    };

    MAT_SNACKBAR_LABEL = (): string => {
        return '.mdc-snackbar__label';
    };

    APOLLO_FEEDBACK_CARD_LABEL = (): string => {
        return '.feedback-card__label';
    };
}
